name: shared_core
description: Shared models and domain logic for Mission apps (mobile and web)
version: 0.0.1
publish_to: 'none'

environment:
  sdk: ">=3.8.0 <4.0.0"

dependencies:
  meta: ^1.12.0
  collection: ^1.18.0

dev_dependencies:
  test: ^1.24.0


